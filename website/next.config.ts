import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Defaults are intentional — the site is a statically prerendered
  // single page; media is synced into public/ by scripts/sync-assets.mjs.
};

export default nextConfig;
